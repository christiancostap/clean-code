{
  "name": "clean-code",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "jest --silent --colors --noStackTrace --passWithNoTests",
    "test:unit": "yarn test -- --watch --onlyChanged --verbose -c jest-unit-config.js",
    "test:integration": "yarn test -- --watch --onlyChanged --verbose -c jest-integration-config.js",
    "test:staged": "yarn test -- --findRelatedTests",
    "test:ci": "yarn test -- --coverage",
    "prepare": "husky install"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/christiancostap/clean-code.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/christiancostap/clean-code/issues"
  },
  "homepage": "https://github.com/christiancostap/clean-code#readme",
  "devDependencies": {
    "husky": "^6.0.0",
    "jest": "^26.6.3",
    "lint-staged": "^10.5.4",
    "standard": "^16.0.3"
  },
  "standard": {
    "env": [
      "jest"
    ]
  },
  "dependencies": {
    "validator": "^13.6.0"
  }
}
